.list-group
    if categories.length > 0 
        each category in categories 
            a(href=`/categories/${category._id}` class = category._id == SelectedCategory ? "active" : "").list-group-item #{category.name}


